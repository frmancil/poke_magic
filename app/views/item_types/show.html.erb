<h1><%= @item_type.name %></h1>

<%= link_to "Back", "/" %>

<% @item_type.items.each do |item| %>
    <ul>
        <p>Product: <%= item.name %></p>
        <%= image_tag item.image %>
    </ul>
<% end %>


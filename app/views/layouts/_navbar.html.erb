<header>

    <div class='container'>

        <nav>
            <ul>
            <li><%= link_to "Home", "/" %></li>        
            <li><%= link_to "Categories", item_types_path %></li>
            <% if member_signed_in? %>
            <h1>Welcome Home, <%= current_member.username %></h1>
            <%= button_to 'Sign out', destroy_member_session_path, method: :delete %>
        <% else %>
            <h1>Home</h1>
            <%= link_to 'Sign in', new_member_session_path %>
        <% end %>
            </ul>
        </nav>

    </div>

</header>